{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Static site build complete'"
  },
  "deploy": {
    "startCommand": "npx serve@latest -s . -l $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30
  }
}
